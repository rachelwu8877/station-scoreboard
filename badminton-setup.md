# 羽球點名板：設定共用資料

完成後，球友不用登入任何帳號，打開網址就能看、也能直接修改；每一次修改都會記在 Google 試算表的「紀錄」分頁。

## 一、建立共用資料庫（Google 試算表，約 5 分鐘）

1. 到 Google 雲端硬碟新增一份試算表，名稱隨意（例如「羽球點名板資料」）。
2. 上方選單 **擴充功能 → Apps Script**。
3. 把編輯器裡原本的內容全部刪掉，貼上 repo 裡 `badminton-apps-script.gs` 的全部內容，按 💾 儲存。
4. 右上角 **部署 → 新增部署作業**：
   - 左邊齒輪「選取類型」選 **網頁應用程式**
   - 執行身分：**我**
   - 誰可以存取：**所有人**
   - 按 **部署**，第一次會要求授權，照畫面允許即可（出現「Google 尚未驗證這個應用程式」時，點「進階 → 前往（不安全）」，這是你自己的程式）。
5. 複製部署完成後的 **網頁應用程式網址**（`https://script.google.com/macros/s/…/exec`）。

## 二、把網址填進網頁

打開 `badminton.html`，找到這一行：

```js
var API_URL = '';
```

把剛剛複製的網址貼在引號中間，存檔後推上 GitHub。

## 三、開啟 GitHub Pages（公開網址）

1. 到 GitHub 的 repo 頁面 → **Settings → Pages**。
2. Source 選 **Deploy from a branch**，Branch 選放這個檔案的分支、資料夾選 `/ (root)`，按 **Save**。
3. 等一兩分鐘，網址會是：
   `https://rachelwu8877.github.io/station-scoreboard/badminton.html`

把這個網址丟到 Line 群組，大家點開就能用。

## 注意

- **拿到網址的人都能修改**：網頁沒有登入機制，請只分享給球友。
- **之後改了 Apps Script 程式**，要用「部署 → 管理部署作業 → 編輯 → 版本選新版本」更新，網址才會維持不變。
- 別人的修改大約 20 秒內會出現在你的畫面上；切回這個分頁時也會馬上更新。
